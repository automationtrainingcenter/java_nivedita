package finalkeyword;
/*
 * we can declare a variable or method or class as final
 * if we declare a variable as final we can't change it's value and final variable name must be in
 * upper case
 * 
 * if we declare a method as final we can't override that method definition in child class
 * 
 * if we declare a class as final we can't create child classes of that class but it can have a parent class
 */

public class FinalKeywordDemo {
	
	public static void main(String[] args) {
//		FinalVariableDemo.fVar = 20;
	}

}
